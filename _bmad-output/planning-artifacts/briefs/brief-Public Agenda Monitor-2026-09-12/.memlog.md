---
topic: Public Agenda Monitor
updated: 2026-09-12T17:20
---

- (decision) The first real vertical-slice source is the public Bundespräsident appointment calendar URL supplied by the user; an official appointments RSS feed and fallback fixture should be used if direct automated access fails.
- (decision) The demo must run a real scheduled scan daily at 05:00.
- (decision) The hackathon MVP has no authentication; it is an open-access demo.
- (decision) Reviewer feedback—approval/rejection, comments, and score changes—must influence future relevance and priority suggestions for similar events.
- (decision) MVP must visibly handle source-page changes or parsing failures, using source-health errors, retained last-successful data, and labelled fallback fixtures rather than silent deletion.
- (assumption) Use only publicly accessible pages/feeds, retain necessary event metadata and direct source links, and check source-specific terms and access rules before production use.
- (decision) MVP success is demonstrated when all three sources yield normalized candidates, a researcher reviews/approves one with feedback, and an editor sees it in the agenda.
- (change) Applied editorial structure and prose review: surfaced the staged MVP delivery sequence in the executive summary, condensed the problem section, moved the commissioning-context assumption, and clarified the lowest score wording.
- (override) Replace the scheduled 05:00 scan with manual retrieval of events from each source for the MVP; rationale: the team needs a simpler, controllable hackathon implementation.
- (decision) Every event needs a stable unique ID; repeated retrieval must upsert/update the matching event instead of creating a duplicate list entry.
